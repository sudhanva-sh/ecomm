spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: user
    password: user
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: user-service

server:
  port: 8081